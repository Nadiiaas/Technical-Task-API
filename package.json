{
  "name": "api-automation",
  "version": "1.0.0",
  "description": "API test automation task",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --spec \"cypress/e2e/test.cy.js\" --browser chrome",
    "cy:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.17.0"
  }
}
